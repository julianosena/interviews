package com.interview.application.usecase

import spock.lang.Specification

class CreateBookingUseCaseSpec extends Specification {

    def "Trying to test something"(){
        given: "Something"
        def given = "1"

        when: "Another thing"
        def when = "1"

        then: "One is equal 1"
        1 == 1
    }
}
